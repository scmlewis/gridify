# Public base URL of the deployed site (used for canonical, Open Graph,
# Twitter, robots.txt and sitemap.xml). Include the /gridify base path.
# Leave unset to fall back to https://USERNAME.github.io/gridify (placeholder).
VITE_SITE_URL=https://USERNAME.github.io/gridify
